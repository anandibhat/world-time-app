apiVersion: v1
kind: Service
metadata:
  name: world-time-app
  labels:
    app: world-time-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: world-time-app
